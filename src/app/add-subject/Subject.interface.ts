import { Interface } from "readline";

export interface  Subject{
    subjectCode,
    subjectName,
    lectureHours,
    exerciseHours,
    year,
    totalHours,
    classCode,
    createdAt?,
    updatedAt?
}
